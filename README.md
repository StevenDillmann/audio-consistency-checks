# audio-consistency-checks

# 1. Generate Instruction Examples
python scripts/generate_instructions_batch.py --metadata categories/lexical_stress_shift.json --num_samples 10 --model gpt-4o --temperature 0.7

# 2. Generate Audio Examples
